# 医院隔震支座安装与临时约束解除质量闭环

## 项目目标

建设一套面向医院隔震层施工的单体 Web 系统。系统在任务锁定时固化楼栋、隔震单元、轴网、设计支座位、上下支墩连接界面、传力邻接、同步解锁组、材料与实体身份、几何参数、检验阈值和施工程序；随后以不可跳级的位置状态前缀推进复验、安装、座浆、紧固、养护、复测和解锁。核心记录采用只追加事件与安装谱系，使用嵌入式关系数据库实现事务持久化、唯一约束和重启恢复，并以数据库中的逻辑时间、租约和终局屏障支持可确定复现的并发行为。计划包含约20—28个生产Go文件、4—6个有实际职责的包和约2500行有效生产Go代码，控制在2000—3000行；前端为真实单页操作台，具备package.json、提交锁文件和固定版本的确定性构建。公开测试至少分布在4个测试文件中并覆盖不少于12个独立用例。

## 端到端业务流程

1. 设计锁定：质量负责人提交医院楼栋和隔震单元方案，系统校验整数微米几何、坐标变换、孔组兼容性、传力邻接及同步解锁分组，固化设计位置全集、制造摘要、材料清单、抽检映射、性能阈值、紧固程序、养护日程和任务代次；锁定后只能通过新代次更正，不能原位覆盖历史。
2. 进场与安装：按设计位置核验证书、制造批次、型号和唯一编号，完成见证抽样性能复验；安装人员在有效互斥租约内依次提交支承面测量、预埋件复核、吊装就位、临时定位、调平垫片、座浆充填、初紧和终紧证据，成功事务同时完成身份绑定、库存去向、材料扣减、资源占用和阶段推进。
3. 养护与解锁：座浆达到锁定养护逻辑时间后记录沉降复测；系统计算偏心、倾斜、界面高差、承压率和预紧偏差，确认性能抽检及全部位置阶段前缀闭合，再由两名不同且合格的人员独立复核，按同步组及确定顺序解除运输锁具和施工临时约束。
4. 仪器失败重试：全站仪、电子水准仪、测隙仪、扭矩采集器和性能试验机通过脚本化适配器返回读数或稳定故障；拒绝、断连、超时或格式错误仅写入带逻辑时间、调用序号和确定重试次数的待重试记录，不生成测量证据、推进阶段或释放屏障。
5. 异常换装：几何、座浆、紧固、养护沉降或性能复验不合格时，以触发位置为种子，对同一隔震单元内的传力邻接、共享制造批次、同一座浆盘次和同步解锁组求确定闭包，生成唯一影响集合并进入质量隔离；换装为受影响位置建立新代次和新实体绑定，永久记录旧支座拆出后的去向，旧代次迟到回执只能留档。
6. 终局移交：终局仲裁器核对身份及数量守恒、安装前缀、有效证据、异常复验、双人复核和同步解锁顺序；移交、质量隔离与取消使用同一个条件写入屏障竞争，唯一胜者生成终局事件，移交胜者同时生成唯一且可重建的隔震层移交凭据。

## 核心组件与职责

1. 隔震设计与材料规则目录：管理锁定快照、整数微米几何、连接界面方向、孔组、制造构造摘要、阈值、抽检映射、紧固及养护规则；提供带溢出检查的定点计算和确定排序校验。
2. 安装任务及身份谱系聚合：维护“隔震单元—设计支座位—实体支座—上下连接界面”的只追加谱系、位置阶段前缀、任务及换装代次，并通过持久化事件和投影支持重启恢复。
3. 一次性构件与资源租约管理器：维护支座、运输锁具、锚栓、垫片和座浆材料的数量与去向守恒；为吊装位、测量通道、扭矩工具、座浆工位和性能试验机提供基于逻辑时间的限时互斥租约。
4. 测量座浆紧固及养护证据记录器：接收人工或脚本化仪器结果，校验阶段、租约、代次、材料时效和定点指标，以只追加形式记录有效证据、失败调用和确定重试计划。
5. 换装复验与终局仲裁器：计算唯一影响闭包，建立换装代次并隔离旧回执；执行双人复核、同步组解锁排序、单写终局竞争和移交凭据生成。
6. Go HTTP API：提供JSON接口、稳定错误响应、幂等操作入口和事务边界，托管构建后的前端操作台；前端展示单元谱系、位置阶段、租约、待重试仪器调用、影响集合、双人复核及终局结果。

## 领域规则与不变量

1. 所有长度、坐标、标高和有效尺寸以有符号整数微米保存；角度、刚度、阻尼、压缩率、承压率及偏差采用明确比例尺的整数定点数。坐标变换、平方和、乘加、绝对值、除法和舍入必须预检溢出、最小整数取反、除零、符号和闭区间边界，统一采用向最接近值舍入且正中值远离零。
2. 锁定快照必须包含完整且不重复的设计位置。位置具有楼栋、隔震单元、轴网、设计坐标、朝向、支承标高、允许偏心及倾斜、上下界面、预埋板和锚栓孔组；缺位、重位、退化或负尺寸、不可逆坐标变换、方向颠倒及孔组不相容均拒绝锁定。设计或制造摘要版本陈旧返回稳定错误码。
3. 实体支座编号全局唯一且同时最多占用一个当前设计位置；型号、制造摘要和接口必须匹配。拆换不得改写原绑定，而是建立递增代次、新实体绑定和旧实体确定去向。运输锁具、锚栓及垫片逐件记录身份，垫片总厚度、层数和组合不得超过锁定上限。
4. 每个位置的有效阶段严格构成“进场合格、基面合格、就位、调平、座浆、初紧、终紧、养护、复测、解锁”的前缀。重复提交可通过相同操作号和相同规范化内容幂等返回；操作号相同但内容不同、跳级、错误代次、错向或过期租约均不得形成有效证据。
5. 实体绑定、一次性构件去向、整数克座浆材料扣减、互斥租约获取和阶段证据写入必须处于同一数据库事务。任意校验、唯一约束或写入失败时整体回滚，不得遗留部分绑定、库存变化、租约、证据、解锁记录或凭据。座浆材料超过锁定可用逻辑时间后不得扣减。
6. 偏心由变换后的设计中心与实测中心差计算，合成倾斜由两个正交分量确定，界面高差按锁定基准比较；座浆有效承压率、锚栓预紧偏差、竖向压缩、水平等效刚度和阻尼均使用规定定点比例、边界及舍入规则。任何指标越界产生稳定不合格原因，而不是推进下一阶段。
7. 异常影响集合限定在同一隔震单元中，由触发位置沿公开传力邻接边扩展，并合并共享制造批次、同一座浆盘次和同步解锁组成员，直至闭包稳定；成员按楼栋、隔震单元、轴网、设计位置、实体编号、界面和代次排序后计算唯一摘要，重复触发不得创建第二个等价集合。
8. 解除运输锁具和临时约束前，所有位置须达到复测阶段，守恒检查、几何证据、养护证据、性能抽检和异常换装复验均合格，并具备两名不同合格人员的独立复核。解除按锁定的同步组顺序及组内位置排序执行；移交、质量隔离和取消共享单写终局屏障，并发时最多一个成功。

## 数据模型与持久化

1. DesignSnapshot：任务代次、楼栋、隔震单元、摘要版本、坐标变换、位置全集、传力邻接、同步解锁组、抽检映射、阈值、材料批次、紧固程序、养护日程及锁定摘要。
2. DesignPosition与ConnectionInterface：轴网、位置编号、设计中心和标高、朝向、支座型号、上下支墩界面、预埋板尺寸、孔组、允许偏心和倾斜；以快照标识和位置编号形成稳定主键。
3. PhysicalComponent与LineageEvent：实体支座、锁具、锚栓和垫片的唯一编号、制造批次、构造摘要、状态、当前位置、去向；谱系事件记录绑定、拆出、换入及代次，历史不可更新或删除。
4. InstallationTask与StageEvidence：位置当前代次、派生阶段投影、证据类型、规范化负载摘要、逻辑时间、人员、仪器调用、租约和判定；有效阶段由只追加证据重放获得。
5. ConsumableLot与InventoryMovement：座浆盘次、初始整数克数、可用截止逻辑时间及只追加扣减/退回流水；约束所有流水汇总与初始数量一致且去向明确。
6. ResourceLease：资源种类、资源编号、持有操作、位置及代次、获取与到期逻辑时间、释放原因；同一资源的有效时间窗不能重叠，过期租约不能授权证据。
7. InstrumentCall：仪器类型、调用编号、脚本步骤、逻辑时间、尝试次数、状态、故障码、原始载荷摘要和下一重试时间；只有成功且格式有效的调用可被证据引用。
8. ImpactCase与ReplacementGeneration：触发原因、影响位置有序集合、集合摘要、隔离状态、新代次、旧实体去向、复验结果；旧代次回执另存为迟到事件且不改变当前投影。
9. Review、UnlockEvent与TerminalDecision：复核人员及资格快照、独立意见、同步组解除序号、锁具去向、终局类型、条件写入版本和唯一移交凭据摘要。
10. IdempotencyRecord与DomainEvent：作用域、操作号、请求规范化摘要、已提交响应、事件序号和逻辑时间；用于重试复用、内容冲突检测、审计及数据库重启后的投影校验。

## 公开接口

1. POST /api/v1/isolation-units：创建并锁定隔震单元设计快照；返回快照摘要或如STALE_SUMMARY、INVALID_GEOMETRY、ARITHMETIC_OVERFLOW等稳定错误码。
2. POST /api/v1/units/{unit}/positions/{position}/operations：以操作号、预期代次、逻辑时间、租约请求、构件身份、材料用量和证据负载原子执行下一阶段；幂等成功返回原响应，内容冲突返回IDEMPOTENCY_CONFLICT。
3. POST /api/v1/leases/acquire与POST /api/v1/leases/{lease}/release：获取或释放限时互斥资源租约；竞争者得到LEASE_BUSY，过期授权得到LEASE_EXPIRED，响应不依赖机器墙钟。
4. POST /api/v1/instrument-calls与POST /api/v1/instrument-calls/{call}/retry：驱动固定脚本的仪器调用，公开成功、待重试和稳定故障信息；测试可通过请求中的脚本标识复现拒绝、断连、超时及格式错误。
5. POST /api/v1/units/{unit}/impacts：登记不合格并生成确定影响闭包；POST /api/v1/units/{unit}/replacements创建下一代次换装任务，拒绝代次冲突及旧代次有效写入。
6. POST /api/v1/units/{unit}/reviews、POST /api/v1/units/{unit}/unlock和POST /api/v1/units/{unit}/terminal：提交独立复核、按序解锁及竞争移交/隔离/取消终局；所有业务拒绝采用统一JSON结构code、message、ordered_reasons和operation_id。
7. GET /api/v1/units/{unit}与GET /api/v1/units/{unit}/lineage：返回阶段矩阵、构件去向、材料余额、租约、待重试调用、影响集合、复核、解锁和终局；所有集合使用规定领域键确定排序。
8. GET /提供构建后的前端单页操作台；前端使用固定版本Node依赖，提交package.json及npm锁文件，通过npm ci和固定构建命令产生确定资源，并经上述HTTP接口完成锁定、取证、换装和移交操作。

## 失败边界

1. 请求解析、业务校验和持久化提交分层处理；只有数据库事务提交后才返回成功。事务内部任何身份、库存、租约、阶段或唯一约束失败均回滚，错误原因在事务外按规定领域键重新排序。
2. 使用嵌入式关系数据库的外键、唯一索引、检查约束和即时事务保护实体占位、操作号、影响摘要及终局唯一性；进程重启后从快照和只追加事件重建投影，并使未过期逻辑租约及待重试调用继续有效。
3. 并发获取租约、绑定实体或提交终局时，失败方只得到LEASE_BUSY、COMPONENT_ALREADY_BOUND、GENERATION_CONFLICT或TERMINAL_ALREADY_DECIDED等稳定结果，不做隐式重试，也不产生半成品记录。
4. 外部仪器适配器与领域事务隔离：失败调用可以持久化为待重试事实，但仪器原始失败不得被转换成业务读数；重试次数和下一逻辑时间由锁定策略确定。
5. 旧代次、迟到仪器结果和过期租约均可作为审计事件保存，但不能加入有效证据投影、改变当前阶段、撤销隔离、释放同步组或覆盖终局。
6. 定点算术集中于可审计模块，所有输入先做范围和符号检查，每次乘加、平方、比例换算及除法均检测溢出；算术失败拒绝整个业务事务并返回字段级有序原因。

## 验收标准

1. 锁定操作完整固化隔震单元、设计位置全集、传力邻接、同步解锁组、构件身份、制造摘要、安装几何、座浆盘次、紧固程序、养护日程、抽检映射、阈值及任务代次；陈旧摘要或不完整拓扑不得落库。
2. 所有外形、中心、标高、朝向、倾斜、孔位和界面尺寸以整数微米确定校验；公开边界用例能够稳定拒绝缺位、重位、退化或负尺寸、非法变换和乘加溢出。
3. 实体支座、锁具、锚栓、垫片及整数克座浆材料在绑定、扣减和租约竞争中保持身份唯一及数量去向守恒；相同操作号同内容重试幂等，内容冲突或任一步失败时数据库状态与请求前一致。
4. 各位置只能按十阶段前缀推进；型号错配、界面颠倒、孔组不容、垫片超限、错向、跳步、失效浆料、错误代次或过期租约均返回稳定错误码且不写有效证据。
5. 偏心、合成倾斜、界面高差、座浆承压率、预紧偏差、竖向压缩、水平等效刚度和阻尼的固定输入产生精确整数结果，并正确处理舍入中点、符号、除零、闭区间和溢出。
6. 脚本化仪器发生拒绝、断连、超时或格式错误时只增加确定的待重试调用；重新启动服务后调用次数和下一逻辑时间保持一致，且位置阶段和同步解锁屏障未推进。
7. 任一规定不合格能够一次性生成按规则排序的唯一影响闭包；换装建立新代次并保留旧支座去向，旧代次迟到回执不改变当前结论或复验状态。
8. 只有全部守恒、位置前缀、几何养护证据、性能及换装复验和两名不同合格人员复核满足时，才能按确定顺序解锁并生成唯一移交凭据；移交、隔离和取消并发竞争最终仅有一个终局。

## 确定性测试场景

1. 锁定虚构医院A楼隔震单元，断言轴网位置、公开邻接表、同步解锁组和摘要在重启前后完全一致。
2. 以缺失位置、重复位置、负尺寸、退化预埋板和非法坐标矩阵分别锁定，断言稳定错误码及有序原因且无快照残留。
3. 使用固定整数微米坐标完成旋转和平移，精确断言中心偏心、标高差和朝向；另以极值乘加触发ARITHMETIC_OVERFLOW。
4. 两个并发操作将同一实体支座绑定到不同设计位置，断言仅一方成功，失败方未扣浆料、未获得租约且未写阶段证据。
5. 对同一操作号提交相同规范化内容两次，断言返回相同事件和余额；更改垫片编号后重试，断言IDEMPOTENCY_CONFLICT。
6. 并发争抢同一吊装位和扭矩工具，断言每个资源在给定逻辑时间只有一个有效租约；到期后新持有者可获取而旧持有者不能推进。
7. 从进场合格直接提交调平，或在座浆材料过期后提交座浆，断言阶段仍停留在原前缀且库存不变。
8. 提交上下连接界面颠倒、孔组错位、型号错配和垫片叠放越限，断言按界面及实体编号确定排序的拒绝原因。
9. 以固定测量值断言合成倾斜、承压率和预紧偏差的中点舍入与阈值闭区间；以零分母和超大刚度输入断言安全拒绝。
10. 脚本化全站仪依次返回超时、格式错误和成功，断言前两次只产生待重试记录，第三次才生成唯一测量证据并推进阶段。
11. 触发制造批次性能不合格，断言影响集合合并邻接位置、同批位置、同盘座浆位置及同步解锁组，重复触发得到同一集合摘要。
12. 完成换装新代次后提交旧代次终紧回执，断言其仅进入迟到审计事件，新代次阶段及当前实体不变。
13. 在事务末端注入唯一约束失败，断言实体绑定、锚栓及垫片去向、浆料余额、租约、证据和解锁记录全部回滚。
14. 由同一人员提交两次复核，断言不能满足双人条件；换用另一名合格人员后，按同步组顺序允许解锁。
15. 在多个协程中同时请求移交、质量隔离和取消，断言数据库中恰有一个终局事件，后续所有请求返回同一已决终局。
16. 完成全部合格流程后生成移交凭据，关闭并重启后端，断言谱系、材料余额、阶段、解锁次序、终局和凭据摘要均可恢复且一致。

## 组件追踪关系

1. 隔震设计与材料规则目录对应锁定流程及验收1、2、5，主要由设计快照、位置/界面模型、规则校验和定点算术文件实现。
2. 安装任务及身份谱系聚合对应进场安装、阶段前缀和换装代次，覆盖验收2、3、4、7，并通过事件仓储、投影、状态机和谱系服务形成自然代码规模。
3. 一次性构件与资源租约管理器对应身份及材料守恒、并发原子绑定和逻辑租约，覆盖验收3、4以及租约竞争和事务回滚测试。
4. 测量座浆紧固及养护证据记录器对应施工证据、定点指标及仪器故障重试，覆盖验收4、5、6。
5. 换装复验与终局仲裁器对应影响闭包、新代次隔离、双人复核、同步解锁和单写终局，覆盖验收7、8。
6. Go HTTP API及前端操作台贯穿全部流程，负责稳定错误协议、幂等入口、确定排序和真实页面交互；预计按领域、应用、持久化、HTTP、仪器适配和入口组织4—6个包、20—28个生产Go文件，公开测试至少拆为geometry_test.go、lineage_transaction_test.go、lease_stage_test.go、impact_terminal_test.go等4个文件。

## 独特性

项目聚焦医院隔震层中实体支座、上下连接界面、运输锁具和同步解锁组的安装谱系，把整数微米几何、一次性材料守恒、限时互斥施工资源、异常传力影响传播与临时约束解除终局整合为一条闭环；其对象、状态序列和仲裁条件均不同于已有的钢结构节点、套筒灌浆、桥梁湿接缝及其他施工移交项目。
